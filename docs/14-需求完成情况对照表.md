# cpp-ai-agent —— 需求完成情况对照表

> 对照表格：[AI编程智能体 需求清单]  
> 日期：2026-07-13

## 一、功能点完成情况

| # | 功能点 | 状态 | 当前实现 | 未完成 / 限制 |
|---|--------|------|----------|---------------|
| 1 | 任务输入与上下文管理 | ✅ 完成 | `Session` + `ContextManager` — 消息存储、system prompt 保留、token 预算滑动窗口 | — |
| 2 | 多轮对话与任务状态跟踪 | ✅ 完成 | `AgentLoop::runTurn()` — 循环中跟踪 assistant 消息和 tool 结果，最大轮次保护 | — |
| 3 | 本地文件读取、写入和编辑 | ✅ 完成 | `ReadFileTool` / `WriteFileTool` / `EditFileTool`，路径越界防护 | — |
| 4 | Shell 命令执行与结果回显 | ✅ 完成 | `ShellTool` — 执行命令并返回 stdout/stderr + 退出码 | 不支持交互式命令（如 `vim`） |
| 5 | Web 搜索与检索结果整理 | ✅ 完成 | `WebSearchTool` — Bing RSS 优先 + DuckDuckGo 兜底 + TTL 5min 缓存 | — |
| 6 | Skill 注册、调用与结果管理 | ✅ 完成 | `SkillCatalog` — `/use-skill` / `/clear-skill`，双重约束（提示词 + ToolPolicy 硬拦截），4 个内置 Skill | — |
| 7 | MCP 服务接入与调用 | ⚠️ 部分 | Tools 完整闭环（initialize / tools/list / tools/call + 持久会话 + AgentLoop 适配） | Resources、Prompts 未实现；仅 stdio，无 SSE/HTTP transport |
| 8 | 执行日志记录与历史回放 | ✅ 完成 | `JsonLogger` — JSONL 格式 + `HistoryReader` — 列表 / 回放 / `/load` 续接 + 会话标题命名 | — |
| 9 | 危险操作提示与权限控制 | ✅ 完成 | `PermissionManager` — Safe / Write / Dangerous 三级 + 可选中确认 UI + diff 预览 | — |
| 10 | TUI 界面展示任务过程 | ⚠️ 部分 | ANSI 流式控制台 — 分层语义输出、SSE 逐 token 渲染、权限交互菜单 | 非全屏 TUI（FTXUI 未启用）；行式阻塞式交互 |

## 二、技术栈对照

| 要求 | 状态 | 当前实现 | 说明 |
|------|------|----------|------|
| 编程语言 C++ | ✅ 完成 | C++17 | — |
| TUI 终端文本交互界面 | ⚠️ 部分 | ANSI 行式控制台 | FTXUI 已依赖但未启用全屏模式 |
| 数据存储 JSON 或 SQLite | ✅ 完成 | JSON（配置文件）+ JSONL（会话日志） | SQLite 未使用，当前规模下 JSONL 足够 |
| 文件 I/O 操作 | ✅ 完成 | `ReadFileTool` / `WriteFileTool` / `EditFileTool` / `ListDirTool` | — |
| 进程管理 | ✅ 完成 | `ShellTool`（命令执行）、`StdioMcpClient`（子进程 MCP 通信） | — |
| HTTP 请求 | ✅ 完成 | `cpr` 库 — LlmClient（API 调用）+ WebSearchTool（搜索请求） | — |
| 插件式工具调度 | ✅ 完成 | `ITool` 统一接口 + `ToolRegistry` 注册查找 | 新增工具只需实现 `ITool`，不修改 AgentLoop |
| 版本控制 Git | ✅ 完成 | GitHub + GitLab 双远程同步 | — |
| CI/CD Jenkins | ❌ 未完成 | `.gitlab-ci.yml` 已创建 | 无 Jenkins 服务器；GitLab CI 等待配置 runner |
| CI/CD Ansible | ❌ 未完成 | 无 | 无部署目标，当前为本地 exe 直接运行 |
| 代码质量 SonarQube | ❌ 未完成 | 无 | 需 SonarQube 服务器 + `sonar-project.properties` 配置 |

## 三、汇总

| 类别 | 完成 | 部分完成 | 未完成 |
|------|------|----------|--------|
| 功能点 (10 项) | 8 | 2 (MCP Resources/Prompts, TUI) | 0 |
| 技术栈 (10 项) | 8 | 1 (TUI) | 2 (Jenkins/Ansible, SonarQube) |

## 四、待完成清单（优先级排序）

| 优先级 | 项目 | 所需工作量 |
|--------|------|-----------|
| P0 | FTXUI 全屏 TUI（将 ANSI 行式升级为四面板 TUI） | 3-5 天 |
| P0 | SonarQube 接入（`sonar-project.properties` + Quality Gate） | 0.5 天 |
| P1 | GitLab CI Runner 配置 + 流水线跑通 | 1-2 天 |
| P1 | MCP Resources / Prompts 实现 | 4-6 天 |
| P2 | MCP SSE / HTTP Transport | 3-5 天 |
| P2 | Ansible 部署脚本 | 1-2 天 |
| P2 | Jenkins pipeline（已有 GitLab CI 可替代） | 1-2 天 |
