# cpp-ai-agent —— 项目思维导图

> 用于答辩展示、项目复盘和快速讲解。结构参考终端演示记录，从“如何跑起来”扩展到“项目做了什么、怎么实现、如何验收、后续如何扩展”。

## Mermaid 思维导图

```mermaid
mindmap
  root((cpp-ai-agent))
    项目定位
      C++17 终端 AI 编程智能体
      简化版 Coding Agent
      面向课程实践和答辩演示
      可运行
      可测试
      可扩展
      可复现
    快速运行流程
      进入项目目录
        cd D:\cpp-ai-agent
        或 cd D:\Code\aiagent
      配置构建环境
        Visual Studio Build Tools
        MSVC x64 工具链
        CMake
        vcpkg manifest
      CMake 配置
        cmake --preset msvc-vcpkg-debug
        自动安装依赖
        生成 build 目录
      编译项目
        cmake --build --preset msvc-vcpkg-debug
        生成 ai-agent.exe
        生成 ai-agent-tests.exe
      启动程序
        .\build\msvc-vcpkg-debug\ai-agent.exe
        输入自然语言消息
        /exit 退出
      验收测试
        ctest --output-on-failure
        目标 100 percent passed
    用户交互体验
      主对话
        用户输入
        Assistant 回复
        工具调用事件
        工具结果回显
      ANSI 流式控制台
        启动横幅
        用户消息样式
        助手打字机效果
        非交互输出自动降级
      UI 概览
        /ui
        Conversation
        Tools
        Status
        Input 提示
      权限确认
        Safe 工具自动通过
        Write 工具需要确认
        Dangerous 工具需要确认或拦截
        y yes 应用变更
        n no 拒绝变更
        直接回车默认拒绝
      Diff 预览
        write_file 写入前预览
        edit_file 编辑前预览
        显示新文件内容
        显示替换片段
        用户确认后才执行
    核心功能
      LLM 对话
        OpenAI-compatible API
        LinkAPI 示例
        DeepSeek 示例
        .env 配置
        settings.json 配置
      Agent 主循环
        发送上下文
        接收 assistant 消息
        解析 tool_calls
        执行工具
        回填工具结果
        多轮迭代直到最终回答
      本地工具系统
        read_file
        write_file
        edit_file
        run_command
        ToolRegistry 统一注册
        ITool 统一接口
      安全控制
        workspace 路径限制
        权限风险分级
        危险命令拦截
        API Key 不入库
        .env 被忽略
      历史记录
        JSONL 会话日志
        /history 查看日志
        /replay 回放历史
    MCP 集成
      最小 stdio 客户端
        initialize
        notifications/initialized
        tools/list
        tools/call
      内置测试 Server
        /mcp-test-server
        echo 工具
        project_info 工具
      演示命令
        /mcp-demo
        /mcp-call-demo
        /mcp-connect command args
      AgentLoop 接入
        McpToolAdapter
        mcp_echo
        mcp_project_info
        模型可自动调用 MCP 工具
      外部 MCP 配置
        config/mcp_servers.json
        enabled 控制启用
        自动发现工具
        注册为 mcp_server_tool
    模块结构
      src main.cpp
        命令入口
        主对话循环
        工具注册
        权限提示
      src agent
        AgentLoop
        工具调用编排
        最大轮次保护
      src core
        Message
        Session
        ContextManager
      src llm
        LlmClient
        LlmParsing
        tool_calls 解析
      src tools
        FileTools
        ShellTool
        ToolRegistry
        PathUtils
        ITool preview
      src security
        PermissionManager
        风险级别
        危险命令识别
      src storage
        JsonLogger
        HistoryReader
      src ui
        Console
        颜色检测
        交互输出检测
      src mcp
        McpClient
        McpToolAdapter
    配置与依赖
      CMakeLists.txt
        可执行目标
        测试目标
        编译选项
      CMakePresets.json
        msvc-vcpkg-debug
        vcpkg toolchain
      vcpkg.json
        cpr
        nlohmann-json
        doctest
        ftxui
      config settings.json
        base_url
        model
        workspace
        permission_mode
      config mcp_servers.json
        外部 stdio server
        默认 disabled
      examples
        LinkAPI env
        DeepSeek env
    测试体系
      config_tests
        环境变量
        配置优先级
      diagnostics_tests
        /config
        /doctor
      context_manager_tests
        system prompt 保留
        上下文窗口截断
      llm_parsing_tests
        assistant 消息
        tool_calls 解析
      tools_tests
        文件读写
        局部编辑
        路径越界防护
        diff preview
      security_tests
        Safe 自动允许
        Write 确认
        Dangerous 拦截
        preview 传入确认回调
      agent_loop_tests
        mock LLM
        工具调用分发
        工具结果回填
        最大轮次保护
      mcp_tests
        MCP 请求生成
        MCP 响应解析
      history_tests
        日志列表
        历史回放
    演示路径
      基础启动演示
        构建
        运行
        输入一句话
        查看 assistant 回复
      配置诊断演示
        /config
        /doctor
      工具调用演示
        读取 README
        创建 temp-demo.txt
        展示 diff preview
        选择 y 确认
      MCP 演示
        /mcp-demo
        /mcp-call-demo
        主对话调用 mcp_project_info
      历史演示
        /history
        /replay logs\session-xxx.jsonl
      答辩讲解演示
        先讲运行闭环
        再讲架构分层
        最后讲测试和扩展
    Git 与交付
      本地 Git
        main 分支
        每次改动后 commit
      GitHub
        origin main
        开发后同步
      GitLab
        gitlab main
        main 受保护时推同步分支
        通过 MR 合并
      不提交内容
        build
        logs
        .env
        本地 settings 修改
    已知边界
      /search 仍是占位
      MCP 暂支持 stdio
      暂未支持 resources
      暂未支持 prompts
      暂未支持 SSE HTTP transport
      真实 API 调用依赖有效 Key
    后续扩展
      真实 Web 搜索工具
      更强 MCP 生命周期管理
      Anthropic-compatible API
      命令白名单
      更细粒度日志脱敏
      更丰富终端交互组件
```

## 讲解顺序建议

1. 先从终端记录切入：项目能配置、能构建、能运行、能对话。
2. 再讲主线闭环：用户输入、模型决策、工具调用、权限确认、结果回填。
3. 然后展开工程结构：`agent`、`llm`、`tools`、`security`、`ui`、`mcp`、`storage`。
4. 接着讲安全与可控性：路径限制、风险分级、diff 预览、选项式确认。
5. 最后讲测试和交付：自动化测试、历史回放、GitHub/GitLab 同步、后续扩展。
