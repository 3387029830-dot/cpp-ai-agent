# cpp-ai-agent —— 版本日志

## v0.1.0 - 2026-07-06

### 总览

v0.1.0 是第一个完整演示版，覆盖 M0-M5：

- CMake + vcpkg 工程骨架。
- OpenAI-compatible LLM 对话。
- Agent 工具调用主循环。
- 本地文件/命令工具。
- 权限确认和日志回放。
- FTXUI 状态页、诊断命令和演示材料。

### 关键提交

| 提交 | 内容 |
|------|------|
| `114a228` | M0: initialize CMake project skeleton |
| `65aa612` | M0.5: add vcpkg-based project scaffolding |
| `3832250` | M1: add basic LLM chat client |
| `c06bb5c` | M2: add local tool system |
| `e6f8a57` | M3: add agent tool-call loop |
| `c23bf2c` | M4: add permissions and JSON logging |
| `3dc21e6` | M4: add history replay |
| `2cbd74c` | M5: add configuration diagnostics |
| `a9eb4b7` | M5: complete delivery features |

### 运行入口

```powershell
.\build\msvc-vcpkg-debug\ai-agent.exe
.\build\msvc-vcpkg-debug\ai-agent.exe /doctor
.\build\msvc-vcpkg-debug\ai-agent.exe /demo
.\build\msvc-vcpkg-debug\ai-agent.exe /ui
.\build\msvc-vcpkg-debug\ai-agent.exe /history
```

### 已知边界

- `/ui` 仍是状态页，不是完整聊天 TUI。
- `/search` 和 `/mcp-demo` 是演示占位。
- 真实 API 调用需要有效 API Key。
- 当前 HTTP 客户端面向 OpenAI-compatible API。
